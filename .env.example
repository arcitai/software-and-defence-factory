# Documentation only. No secrets or paid execution defaults.
PORT=4317
FACTORY_DEMO=1
# FACTORY_DB=/private/factory/state.sqlite
# FACTORY_REPO=owner/repository
# GITHUB_TOKEN=set privately on the server
# GITHUB_WEBHOOK_SECRET=set privately on the server
# FACTORY_MAINTAINERS=trusted-github-login
# Worker-only: FACTORY_WORKER_ISOLATED, FACTORY_CODEX_HOME, FACTORY_MODEL
# Optional provider credential: KASTANJE_API_KEY (worker environment only)
