# One observable change

Synthetic installation qualification only: change `value.txt` from `broken` to
`fixed`. The configured check must fail before the change and pass afterwards.
Preserve all other behavior. Do not publish, merge or deploy anything.

For your own app, replace this with one vertical slice: user-visible outcome,
reproduction, acceptance checks, affected data/access and explicit boundaries.
